CREATE TABLE program_info
  (ages varchar(17),
   deadline integer,
   fee integer,
   subject varchar(50),
   country varchar(20),
   city varchar(30),
   minimum_cost integer,
   program_url varchar(350);


.import data.csv program_info
